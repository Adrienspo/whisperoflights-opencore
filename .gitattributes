# See https://git-scm.com/docs/gitattributes for more about git attribute files.

# Set the default behavior for all files to automatically handle line endings.
* text=auto

# End-of-line normalization preferences and diffs handling:

# Source files:
*.c     text diff=cpp
*.cc    text diff=cpp
*.cxx   text diff=cpp
*.cpp   text diff=cpp
*.cpi   text diff=cpp
*.c++   text diff=cpp
*.hpp   text diff=cpp
*.h++   text diff=cpp
*.hh    text diff=cpp
*.h     text diff=cpp linguist-language=C++
*.h.in  text diff=cpp linguist-language=C++
# Specify C++ as the language for .h and .h.in files to ensure proper language detection
# and syntax highlighting on GitHub, as these extensions can be ambiguous between C and C++.

# Configuration files:
CMakeLists.txt text eol=lf
.clang-* text       eol=lf
.editorconfig       eol=lf

# Documentation:
*.md    text diff=markdown eol=lf
LICENSE text               eol=lf

# Binary files: Explicitly declare binary file types to prevent Git from
# attempting to normalize their line endings, and thus avoid corruption.

# Compiled object files:
*.slo   binary
*.lo    binary
*.o     binary
*.obj   binary

# Precompiled headers:
*.gch   binary
*.pch   binary

# Compiled dynamic libraries:
*.so    binary
*.dylib binary
*.dll   binary

# Compiled static libraries:
*.lai   binary
*.la    binary
*.a     binary
*.lib   binary

# Executables:
*.exe   binary
*.out   binary
*.app   binary
